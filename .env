# =============================================================================
# Mongo Join Pivot - Docker Compose 部署配置
# =============================================================================

# 服务端口配置
BACKEND_PORT=8000
FRONTEND_PORT=3000

# =============================================================================
# 数据库配置 (可选 - 用于配置持久化存储)
# =============================================================================

# MongoDB连接配置
MONGO_URI=mongodb://host.docker.internal:27017
MONGO_DB=ozondatas

# 允许访问的集合 (用逗号分隔，* 表示允许所有)
ALLOWED_COLLECTIONS=*

# 透视配置存储集合名称
PIVOT_PREFS_COLLECTION=pivot_prefs

# =============================================================================
# API配置
# =============================================================================

# 前端API基础地址 (Docker内部通信)
API_BASE=http://backend:8000

# 外部访问API地址 (用于开发调试)
EXTERNAL_API_BASE=http://localhost:8000

# =============================================================================
# 应用配置
# =============================================================================

# 应用运行模式 (development/production)
APP_MODE=production

# 是否启用API文档 (true/false)
ENABLE_DOCS=true

# 日志级别 (DEBUG/INFO/WARNING/ERROR)
LOG_LEVEL=INFO

# =============================================================================
# 文件上传配置
# =============================================================================

# 最大文件上传大小 (MB)
MAX_FILE_SIZE_MB=500

# 最大数据集行数
MAX_DATASET_ROWS=10000000

# 临时文件存储路径
TEMP_DIR=/tmp/pivot_temp

# =============================================================================
# 性能配置
# =============================================================================

# 数据处理块大小
CHUNK_SIZE=10000

# 最大并发处理数
MAX_WORKERS=4

# 缓存TTL (秒)
CACHE_TTL=3600

# =============================================================================
# 安全配置
# =============================================================================

# CORS允许的源 (用逗号分隔，* 表示允许所有)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# 会话密钥 (生产环境请使用复杂密钥)
SECRET_KEY=your-secret-key-change-in-production

# =============================================================================
# 监控和健康检查
# =============================================================================

# 健康检查间隔 (秒)
HEALTH_CHECK_INTERVAL=30

# 是否启用性能监控
ENABLE_MONITORING=true

# =============================================================================
# Docker特定配置
# =============================================================================

# 容器时区设置
TZ=Asia/Shanghai

# 容器重启策略
RESTART_POLICY=unless-stopped
